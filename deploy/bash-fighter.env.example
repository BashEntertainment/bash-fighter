# Copy to /srv/bash-fighter/shared/bash-fighter.env on the box (not
# committed -- that file is real box config, this is just the shape).
# Referenced by deploy/systemd/bash-fighter.service via EnvironmentFile.

PORT=8081
NODE_ENV=production
